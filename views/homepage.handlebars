<div class = "container">
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>Have an idea? Create a post and let the world see it!</h2>
  </div>
  <div class="col-md-6">
    <h3>Here is the place where you can get some feedback on your projects, ask questions and see what others are working on.</h3>
  </div>
</div>

{{#each posts as |post|}}
  <div>
    <h2>{{post_header}}</h2>
    <p>Posted by: {{user_id}}</p>
    <p>{{body}}</p>
   <p>Comments: {{post.comments.length}}</p>
    <a href="/api/posts/{{id}}">View Post</a>
    </div>
{{/each}}

<div class = "postArea"> 
<div id = "post-form">
  <div >
    <h3>Add a New Post!</h3>
    <form class="form">
       <p>Post Title:</p>
      <div class="form-group ">
        <label for="post">
        </label>
        <textarea class="form-input" id="postTitle" name="post-title"></textarea>
         <p>Post Content:</p>
        <textarea class="form-input" id="post" name="post-content"></textarea>
      </div>
      <div class="form-group mx-auto" id="post-button">
        <button type="submit" class="btn btn-primary">Add</button>
      </div>
    </form>
  </div>
</div>
</div>
    
</div>

</div>

<script src="/js/post.js"></script>